注文履歴一覧
<table>
  <tr>
    <th>注文日</th>
    <th>配送先</th>
    <th>注文商品</th>
    <th>支払金額</th>
    <th>ステータス</th>
    <th>注文詳細</th>
  </tr>
  <% @orders.each do |order| %>
  <tr>
    <td><%= order.created_at %></td>
    <td><%= order.postal_code %></br>
        <%= order.address %></br>
        <%= order.name %></td>
    <td>
       <% order.order_details.each do |order_detail| %>
          <%= order_detail.item.name %>
       <% end %>
    </td>
    <td>１５００</td>
    <td><%= order.status %></td>
    <td><%= link_to "表示する" ,"/orders/#{order.id}" ,class:"btn btn-success" %></td>
  </tr>
  <% end %>

  </div>




