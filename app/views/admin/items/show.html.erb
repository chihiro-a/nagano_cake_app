<p><span style="background-color:	#e8e8e8">　商品詳細　</span></p>
<div class="container">
    <div class="row">
        <div class="col-4">
            <%= attachment_image_tag @item, :image, :fill, 200, 200, format: 'jpeg'%>
        </div>
        <div class="col-8">
            <table>
    <tr>
        <th>商品名<td><%= @item.name %></td></th>
    </tr>
    <tr>
        <th>商品説明<td><%= @item.introduction %></td></th>
    </tr>
    <tr>
        <th>ジャンル<td><%= @item.genre.name %></td></th>
    </tr>
    <tr>
        <th>税込価格<br>(税込価格）<td><%= ((@item.price*1.1).round).to_s(:delimited) %>(<%= @item.price.to_s(:delimited)%>)円</td></th>
    </tr>
    <tr>
        <th>販売ステータス<td>
        <% if @item.is_active == true %>
        <p class="text-success">販売中</p>
        <% else %>
        <p class="text-secondary">販売停止中</p>
        <% end %>
        </td></th>
    </tr>
</table>
        </div>
    </div>
</div>


<div class="text-center pt-5">
    <%= link_to "編集する" ,edit_admin_item_path(@item.id) ,class:"btn btn-success "%>
</div>
