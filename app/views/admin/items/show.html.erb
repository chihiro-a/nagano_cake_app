商品詳細画面
<div>
    <%= attachment_image_tag @item, :image, :fill, 100, 100, format: 'jpeg'%>
</div>
<table>
    <tr>
        <th>商品名<td><%= @item.name %></td></th>
    </tr>
    <tr>
        <th>商品説明<td><%= @item.introduction %></td></th>
    </tr>
    <tr>
        <th>ジャンル<td><%= @item.genre.name %></td></th>
    </tr>
    <tr>
        <th>税込価格<br>(税込価格）<td><%= @item.price %></td></th>
    </tr>
    <tr>
        <th>販売ステータス<td>
        <% if @item.is_active == true %>
        <%= "販売中" %>
        <% else %>
        <%= "販売停止中" %>
        <% end %>
        </td></th>
    </tr>
</table>

<%= link_to "編集する" ,edit_admin_item_path(@item.id) ,class:"btn btn-success "%>