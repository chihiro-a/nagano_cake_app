管理者トップページ（注文履歴一覧表示）

<p><span style="background-color:	#e8e8e8">　注文履歴一覧　</span></p>
<table frame="void" rules="rows" border="1" width=100%>
  <tr bgcolor="#e8e8e8" height="40px">
    <th width=25%>購入日時</th>
    <th width=25%>購入者</th>
    <th width=25%>注文個数</th>
    <th width=25%>注文ステータス</th>
  </tr>
  <% @orders.each do |order|%>
  <tr height="40px">
    <td><%= link_to order.created_at.strftime("%Y/%m/%e %H:%M:%S"), admin_order_path(order.id)  %></td>
    <td><%= order.customer.last_name %><%= order.customer.first_name %></td>
    <td><%= order.order_details.all.sum(:amount) %></td>
    <td><%= order.status %></td>
  </tr>
  <% end %>
</table>
<%= paginate @orders %>
